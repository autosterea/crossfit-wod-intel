name: Fetch Daily WOD

on:
  schedule:
    - cron: '0 14 * * *'  # 6am PT (14:00 UTC)
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Fetch latest WOD
        run: node scripts/fetch-daily-wod.mjs

      - name: Check for changes
        id: check
        run: |
          if git diff --quiet src/data/crossfit-data.json; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push
        if: steps.check.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/crossfit-data.json
          git commit -m "Add daily WOD for $(date +%Y-%m-%d)"
          git push
